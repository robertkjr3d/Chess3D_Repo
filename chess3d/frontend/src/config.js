// Backend API Configuration
// Loaded from .env.development (npm start) or .env.production (npm run build) automatically.
// Set REACT_APP_API_URL in the appropriate .env file — no manual commenting needed.
export const API_BASE_URL = process.env.REACT_APP_API_URL ?? '';
